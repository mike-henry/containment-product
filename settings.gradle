pluginManagement {
    resolutionStrategy {
        eachPlugin {

        }
    }
    repositories {
        mavenLocal()
        maven {
            url "${nexusUrl}${relativePublicPath}"
        }
        gradlePluginPortal()
    }
}

include 'containment-product-api'

include 'containment-product-service'
